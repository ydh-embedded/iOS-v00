import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.iOS-v00',
  appName: 'iOS-v00',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
